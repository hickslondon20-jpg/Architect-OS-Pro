# Phase 0 Plan 01 — VCSO Single-Path Consolidation + CLAUDE.md + Endpoint Confirm

> Read the workstream `../../CONTEXT.md` + `../../ROADMAP.md` and this phase's `00-CONTEXT.md` first.
> Covers **CLEAN-1, CLEAN-2, CLEAN-5**. Evidence base: `../../../RECONCILIATION-COGNITIVE-ORCHESTRATION.md`.
> **Verify references before removing anything.** No harness logic; behavior-preserving cleanup only.

## Deliverable
The live VCSO reduced to a single, unambiguous path (Python `/api/vcso/chat`), the dead Vercel VCSO
function retired/quarantined, CLAUDE.md Rule #1 corrected, and the frontend call URL confirmed —
committed version-tagged. No change to live VCSO behavior.

## Steps

### A. Confirm the live path + frontend endpoint (CLEAN-5)
1. Re-verify usage split on live: `ai_usage_log` last 10d shows `surface='virtual_cso'` active and
   `surface='ws5-chat'` = 0 (baseline already captured in the reconciliation doc).
2. Find the frontend's actual VCSO call: locate the chat-send/SSE consumer in `src/` (the reconciliation
   pass could not grep `src/` due to cloud-only files — materialize/read the relevant service/hook).
   Confirm it targets the Python route (`api.architectospro.com/api/vcso/chat` or the configured
   backend base), **not** the Vercel `/api/vcso/chat` function.
3. Record the confirmed endpoint + the config/env var that sets the backend base URL.

### B. Retire or quarantine the dead Vercel VCSO (CLEAN-1)
1. Determine live-vs-dead for **`api/vcso/chat.ts`** and **`api/vcso/writeback.ts`** the same way
   (route references, `vercel.json`/rewrites, frontend calls, recent surface usage).
2. Grep the repo for any reference to these functions (frontend, config, other `api/` files, docs).
3. If **no live reference:** remove the dead file(s). If **any ambiguity/risk:** quarantine instead —
   add a top-of-file deprecation banner (`DEPRECATED — legacy single-shot VCSO, superseded by the
   Python /api/vcso/chat agentic loop; do not extend`) and, if applicable, disable its route so it
   cannot serve. Do not delete under uncertainty.
4. Note in `00-COMPLETION.md` which action was taken and the reference-check evidence.

### C. Correct CLAUDE.md Rule #1 (CLEAN-2)
1. Update Rule #1 (Architecture Rules → AI synthesis lanes) so the Virtual CSO interactive-chat lane
   reads as **Python-backend-served** (`/api/vcso/chat` streaming from `VcsoChatService`), retiring the
   "Vercel serverless streaming exception" wording. If any legitimate Vercel function remains (e.g.
   `os-engine/ingest`, `skills/import-json`), keep the lanes accurate — repoint precisely, don't
   blanket-delete.
2. Keep the edit surgical and factual; do not restructure Rule #1's other lanes.

## Acceptance criteria
1. Frontend VCSO endpoint confirmed = the Python `/api/vcso/chat` path; the config source is recorded.
2. `api/vcso/chat.ts` (+ `writeback.ts` if dead) removed or quarantined, with reference-check evidence;
   **nothing live references the retired code.**
3. Post-change, a live VCSO turn still works and still logs `surface='virtual_cso'`; `ws5-chat` stays 0.
4. CLAUDE.md Rule #1 corrected and accurate for all remaining lanes.
5. Frontend build green (if any `src`/`api` file changed); `python -m compileall python-backend` clean
   (no Python changes expected).
6. `ROADMAP.md` + `STATE.md` updated (O1 resolved); commits version-tagged (PATCH++).

## Out of scope
Router/planner/working-state (P1+); wiki authority + feeder (that is `00-02-PLAN`); any change to the
Python VCSO loop's behavior.
