# Plan 06-01 — Corrections Lifecycle + Activity Log + Health/Lint

**Sub-phase:** 06 — Corrections + Log + Health
**Plan:** 1 of 1
**Depends on:** 03 (engine), 02 (schema)
**Status:** Ready for execution
**Decisions:** `../../CONTEXT.md` (corrections = preserved override; flag-don't-resolve) · **Ref:** `../../REFERENCES.md` L2-7/L2-8/L2-9

---

## Goal

Make the founder-facing surfaces meaningful and the wiki self-maintaining: the **correction
override-preservation lifecycle**, **activity logging**, and **health/lint**. Reuses Layer 1's
override + action-log + health patterns and theafh's emergent-wiki lint.

---

## Pre-Execution Checks
1. Read `../../CONTEXT.md` (corrections preserved across re-synthesis) + `03-01-PLAN.md` (the correction hook).
2. Read `../01-verify-delta/01-01-DELTA.md` §F (`ose_page_corrections`, `ose_activity_log` shapes).
3. Read `../../wiki-system/phases/06-validation-health/` + `phases/07-consolidation/` for the reusable patterns.

## Build

### Corrections (preserved override — CONTEXT)
- A founder correction (`ose_page_corrections`, status `pending`→`applied`) is a **highest-precedence
  override** that **survives re-synthesis**: when the engine regenerates a page, it honors/re-applies the
  correction (preserved block or synthesis constraint — pick one, document it). Founder edits never clobbered.
- Wire the status transitions; corrections are founder-authored only.

### Activity log
- Write `ose_activity_log` events (kind `activity`/`decision`) for ingest, synthesis create/update,
  contradiction-flag, correction, and refresh — so `LogView` shows the real timeline (L2-7).

### Health / lint (L2-8/L2-9, reuse Layer 1)
- Checks over `ose_knowledge_pages`: **contradictions**, **stale** (review_date/observed_date), **orphans**
  (no inbound links), **missing cross-references**, **missing-page-for-mentioned-concept**, broken provenance
  (`source_file_ids` not resolving). Findings only — **no auto-resolve** (A3 / flag-don't-resolve).
- Reuse Layer 1's consolidation `assess→fix→verify` discipline, **write-scoped** (dedup/flag/retire/surface;
  never destroy founder content). Feed findings to the UI (Index/Wiki/Log).

## Success criteria
1. A founder correction survives a re-synthesis of the same page (preserved override).
2. The activity log records ingest/synthesis/correction/contradiction events; `LogView` is meaningful.
3. The health/lint check set runs and surfaces findings without auto-resolving.
4. Orphans / contradictions / stale / broken-provenance are detectable and feed the UI.

## Out of scope
Engine/adapters (03/04), embeddings (05), live CSO/UI wiring (connection phase).
