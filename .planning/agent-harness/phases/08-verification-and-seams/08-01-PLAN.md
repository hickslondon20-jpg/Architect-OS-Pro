# Phase 8 Plan — Verification & Seams (Ep6 Closeout)

> Read build-level `CONTEXT.md` + `ROADMAP.md`, this phase's `08-CONTEXT.md`, and all Phase 1–7
> `0N-COMPLETION.md` first. **No features.** Code-level verification + assertions + the §8 handoff
> checklist. Live-credential smokes are deferred to §8 (L18). No separate `08-RESEARCH.md` — the
> `08-CONTEXT.md` assertion matrix is the spec.

## Deliverables
1. A verification run covering §A end-to-end + §B assertions + §C Ep7 seams (from `08-CONTEXT.md`).
2. `08-COMPLETION.md` with pass/fail per check + the consolidated §8 deferred-smoke checklist.
3. Tracker updates marking Ep6 build code-complete; STATE points to the §8 checklist.

## A. End-to-end (code-level, fakes where live creds are needed)
- Anchor P&L runs to **Review** + `artifact.html` from **each** entry point (Profile, Kanban, VCSO
  `@Agent`); assert all resolve to **one** task object (differ only by `origin`).
- State machine: Ready→Running→Blocked→Review→Done; revision re-enters Running; **no Running→Done
  skip**. Resume a `llm_batch_agents` step + a Blocked task from partial state.
- Graduation registers a `domain_agent_task` artifact (lineage+provenance); `POST /{id}/promote`
  triggers `synthesize_from_task` + sets `promoted_to_kb`; **nothing auto-promotes**.
- Deep Mode ON grants the deep tools; **OFF byte-for-byte unchanged** (assert diff).

## B. Locked-decision assertions (see `08-CONTEXT.md` matrix)
L14 (no Deep Mode in Domain Agents; `@Agent` = handle) · L11 (curated trace only — grep for CoT
leakage) · L12/C1 (Claude orchestration; no `response_format`) · L13 (usage tagged
`surface`+`role`+`task_id`; deep rolls up under `virtual_cso`) · L20 (sandbox export scoping; agent
artifacts not via N8N) · L21 (one `workspace_files` for task+thread) · C2/C4 (tasks first-class;
editable todos Deep-Mode-only). Each assertion is a concrete grep/test/query with evidence.

## C. Ep7 seams
`tasks.step_results` + `artifacts.provenance` carry `source_refs` for the anchor run; the
`synthesize_from_task` promotion payload is well-formed (downstream stubbed OK).

## §8 handoff checklist (produce; do NOT run live here — L18)
Live Anthropic task + Deep turn · GKE deny-all egress NetworkPolicy + bridge + DOCX export (L20) ·
authenticated browser click-through · live OS Engine promotion→wiki+vector (L17) · Ep5 verification
debt (egress/Python-stream flip). Then §8.1 front-end/UX audit + §8.2 consolidated cross-episode
smoke precede go-live.

## Reuse
The Phase 1–7 tests + `HarnessEngine`/`vcso_chat_service`/routers; Supabase live schema checks; no
new code beyond test/assertion scaffolding.

## Acceptance criteria
1. §A end-to-end passes (code-level) from all three entry points; state machine + resumability +
   review gate hold.
2. All §B assertions pass or are logged with the §8 owner; §C Ep7 seams verified.
3. `08-COMPLETION.md` written with per-check evidence + the complete §8 deferred-smoke checklist;
   trackers mark Ep6 build code-complete.

## Out of scope
Any live-credential smoke (→ §8); new features; the front-end/UX audit (§8.1); go-live (§8.2).
