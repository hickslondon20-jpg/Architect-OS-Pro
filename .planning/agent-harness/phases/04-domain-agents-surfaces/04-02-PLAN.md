# Phase 4-02 Plan — Workspace (Live Task Execution)

> Read build-level `CONTEXT.md` + `ROADMAP.md`, `04-RESEARCH.md` + `04-CONTEXT.md`, and `04-01-PLAN.md`
> first. Honors L11, L13, L14, L21, SURF-01/02. **Functional wiring only.** Depends on 04-01 (reads)
> + Phase 2 execution endpoints + Phase 3 P&L workflow. Reuse VCSO components; don't restyle.

## Deliverables
1. Frontend `tasksApi` SSE client (mirror `lib/virtualCsoApi.ts`) against `/api/tasks/*`.
2. `DomainAgentWorkspace` wired live: task-bound thread (reused chat components) + artifact render
   panel + task meta bar + progress indicator + reply/upload + completion actions.
3. Workspace-file read affordance for the render panel (confirm/added).

## Frontend — `lib/tasksApi.ts` (mirror the VCSO SSE client)
- `createTask`, `runTask` (SSE over `POST /api/tasks/{id}/run`, parse the frozen `task_*` events),
  `getTask` (`GET /api/tasks/{id}` reconstruct/resume), `replyTask` (`POST /{id}/messages`),
  `uploadFile` (`POST /{id}/files`), `cancelTask` (`POST /{id}/cancel`).
- Event vocabulary (Phase‑2 frozen): `task_ready`, `task_step_start/complete/error`, `task_blocked`,
  `task_batch_progress`, `task_sub_agent_start/complete`, `task_review`, `task_done`, `task_error`.

## Frontend — `DomainAgentWorkspace` (two-pane, task-bound; reuse `virtual-cso/` components)
- **Main pane:** reuse `ChatThread`/`Composer`/`MessageBubble` for the task thread; agent narrates
  its plan; clarifying questions + resource prompts inline. Founder **reply** unblocks a Blocked
  task; **file upload** satisfies a prereq. Scoped — not open brainstorming (L14).
- **Trace/progress:** reuse `AgentStepsPanel` (curated trace, L11) driven by `task_step_*` /
  `task_sub_agent_*` / `task_batch_progress`; progress indicator = fixed workflow steps
  (going-to-do → in-progress → done → waiting-on-you) — **not** an editable todo panel (C4).
- **Right rail — artifact render panel:** live HTML preview of `artifact.html` (the Phase‑3
  workspace file) as it appears; collapsible (drawer-ready per the scaffold).
- **Task meta bar:** agent, workflow, task id/title, current status (→ Kanban state), attached
  resources.
- **Completion actions (Review/Done):** **Download** artifact (live); **Add to Second Brain**
  present but **inert** (Phase 5 wires the L17 trigger).
- **Entry + resume:** Profile launch / Kanban card both open the same task; on (re)entry
  `getTask` reconstructs and `runTask` streams (start or resume from `current_step`, per L21 durable
  state).

## Workspace-file read affordance
The render panel needs `artifact.html` content. Confirm whether a `GET /api/tasks/{id}/files/{path}`
(or workspace-content in `GET /api/tasks/{id}`) exists; if not, add a minimal founder-auth read
(reuse `workspace_files` RLS). Keep it read-only.

## Acceptance criteria
1. Launching the P&L workflow opens the Workspace; the run streams live (SSE), **Blocks** for
   upload, resumes on upload, renders `artifact.html` in the right rail, reaches **Review**.
2. Reply unblocks; reconnect/return reconstructs state and resumes (L21).
3. Trace is curated only (L11); progress = fixed steps (no editable todos, C4); no open-ended
   brainstorming surface (L14).
4. Download works; Second Brain inert; usage attributed to the task (metering already tagged, L13).
5. `npm run build`/focused TS check + `compileall` pass; live Anthropic/GKE smoke flagged (L18).
6. Trackers + `04-COMPLETION.md` written (combined Phase‑4 completion).

## Out of scope
Read endpoints/Gallery/Profile/Kanban/Library (04-01); promotion trigger (P5); VCSO `@Agent` entry
(P7); net-new assembly (L16 later); visual redesign (§8).
